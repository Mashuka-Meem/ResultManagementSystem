@model ResultManagementSystem.Models.CourseList
@*@{
        Layout = null;
    }*@

@{
    ViewData["Title"] = "View Course Information Page";
}
<!-- Navbar -->
@{
    ViewData["Message1"] = "Register";
    ViewData["MessageR"] = "Register";
    ViewData["Message2"] = "View Course Information";
}

<!-- search -->
<form asp-controller="Admin" asp-action="ViewCourseInformation" class="col-sm-12" method="post">
    <center>
        <div class="form-group">
            <div class="col-sm-6">
                <label>Course Code:</label>
                <input type="text" name="Id" class="form-control">
                <br><br><br>
                <div class="col-md-offset-2 col-md-10">
                    <input asp-action="ViewCourseInformation" type="submit" value="Search" class="btn btn-default" />
                </div>
            </div>
        </div>
    </center>
</form>

<!-- Course list -->
<div class="col-sm-12">
    <div class="table-responsive">
        <table id="table" class="table table-striped table-bordered" cellspacing="0" width="100">
            <thead>
                <tr>
                    <th>Course Code</th>
                    <th>Course Name</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var data in Model.CourseAll)
                {
                    <tr>
                        <td class="showDetails pointer">@data.CourseCode</td>
                        <td class="showDetails pointer">@data.CourseName</td>
                        <td>
                            <a asp-action="UpdateCourseInformation" asp-route-id="@data.CourseCode">Update</a>
                            <a asp-action="DeleteCourseInformation" asp-route-id="@data.CourseCode">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
