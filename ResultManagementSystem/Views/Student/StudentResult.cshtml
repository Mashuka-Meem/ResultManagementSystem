@model ResultManagementSystem.Models.MarksList
@*@{
        Layout = null;
    }*@

@{
    ViewData["Title"] = "Student Result Page";
}

<!-- Navbar -->
@{
    ViewData["Message1"] = "Student";
    ViewData["MessageR"] = "Student";
    ViewData["Message2"] = "View Result";
}

<!-- Result -->
<div class="col-sm-12">
    <div class="table-responsive">
        <table id="table" class="table table-striped table-bordered" cellspacing="0" width="100">
            <thead>
                <tr>
                    <th>Course Code</th>
                    <th>Marks</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var data in Model.MarksAll)
                {
                    <tr>
                        @*<td class="showDetails pointer">@data.Id</td>*@
                        <td class="showDetails pointer">@data.CourseCode</td>
                        <td class="showDetails pointer">@data.Mark</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
